# os / misc
.DS_Store

# machine-local, never-shipped agent context (cross-repo pointers, sandbox paths)
CLAUDE.local.md

# dependencies / build
node_modules/
*.log

# the user's own profile + run output live OUTSIDE the plugin, but ignore them
# here too in case someone keeps theirs inside the repo
/profile/identity.md
/profile/skills-experience.md
/profile/voice-profile.md
/profile/positioning-modes.md
/profile/portfolio-links.md
/profile/principles.md
/profile/resumes/*.pdf
/runs/

# private planning docs (roadmap, specs, decisions, brains-trust runs).
# deliberately NOT public — this repo is, the backlog isn't. see docs/README.md.
/docs/
